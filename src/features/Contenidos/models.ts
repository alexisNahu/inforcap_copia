export interface Contenidos {
    seminarios: {
        clave: string,
        titulo: string,
        descripcion: string,
    },
    talleres: {
        clave: string,
        titulo: string,
        descripcion: string,
    }
}
